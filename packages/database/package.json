{
  "name": "@chathotel/database",
  "version": "1.0.0",
  "private": true,
  "main": "src/seed.ts",
  "scripts": {
    "build": "echo 'Database package - no build needed'",
    "seed": "tsx src/seed.ts",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "prisma": "^6.8.2"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}